type Mutation{
    createUser(input: CreateUserInput): UserOutput
}

type UserOutput{
    id: ID!
    age: Int
    name: String
    email: String
}

input CreateUserInput {
  name: String
  age: Int
  email: String
}
