type LoginResponse{
    id: ID!
    age: Int
    name: String
    email: String
    access_token: String
}

input LoginUserInput {
  email: String
  password: String
}

type Mutation {
  login(loginUserInput: LoginUserInput!): LoginResponse
}